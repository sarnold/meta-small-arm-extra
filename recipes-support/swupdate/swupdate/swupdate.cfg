globals :
{
    verbose         = true;
    loglevel        = 5;
    syslog          = true;
    // public-key-file = "@PUBKEY@";
};


logcolors : {
    error = "red:bold";
    trace = "green:normal";
    debug = "magenta:normal";
    warning = "yellow:underline";
};

download : {
    retries         = 3;
    timeout         = 1800;
};

identify : (
    { name = "raspberryPI"; value = "1.1"; }
);
