header:
  version: 14
  includes:
    - kas/poky/base.yaml

local_conf_header:
  systemd: |
    CORE_IMAGE_EXTRA_INSTALL:append = " systemd-analyze"
    DISTRO_FEATURES:append = " systemd"
    INIT_MANAGER = "systemd"
