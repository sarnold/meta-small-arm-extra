header:
    version: 14

local_conf_header:
    fit: |
        KERNEL_CLASSES += "kernel-fitimage"
        KERNEL_IMAGETYPES += " fitImage "
        IMAGE_BOOT_FILES:append = " fitImage;fitImage-${MACHINE}"
