header:
    version: 14
    includes:
        - kas/oe/arm.yaml
        - kas/oe/ti.yaml
        - kas/oe/vboot.yaml

machine: beaglebone

local_conf_header:
    bbb-bits: |
        EXTRA_IMAGE_FEATURES:append = " tools-debug"
