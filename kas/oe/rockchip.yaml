header:
    version: 9
    includes:
        - kas/oe/arm.yaml

# default is firefly/libre roc-rk3328-cc Rpi clone
machine: roc-rk3328-cc

repos:
    meta-linux-mainline:
        url: "https://github.com/betafive/meta-linux-mainline.git"
        branch: mickledore
        path: "layers/meta-linux-mainline"

    meta-rockchip:
        url: "https://github.com/shr-project/meta-rockchip"
        branch: jansa/mickledore
        path: "layers/meta-rockchip"

local_conf_header:
    rockchip: |
        # we need this until move to branch with linux-yocto 6.6+
        PREFERRED_PROVIDER_virtual/kernel = "linux-stable"
        IMAGE_INSTALL:append = " lirc v4l-utils ntp-utils zram"
