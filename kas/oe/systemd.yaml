header:
  version: 14
  includes:
    - kas/oe/base.yaml
    - kas/oe/rpi64.yaml
    - kas/oe/swu.yaml

local_conf_header:
  systemd: |
    IMAGE_INSTALL:append = " systemd-analyze"
    DISTRO_FEATURES:append = " systemd usrmerge"
    INIT_MANAGER = "systemd"
