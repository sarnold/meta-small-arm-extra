header:
  version: 14
  includes:
    - kas/oe/base.yaml
    - kas/oe/bpi.yaml

local_conf_header:
  systemd: |
    CORE_IMAGE_EXTRA_INSTALL:append = " systemd-analyze"
    DISTRO_FEATURES:append = " systemd usrmerge sysvinit"
    INIT_MANAGER = "systemd"
