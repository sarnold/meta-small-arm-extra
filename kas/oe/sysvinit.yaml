header:
  version: 9
  includes:
    - kas/oe/base.yaml

local_conf_header:
  harden: |
    PACKAGE_CLASSES = "package_rpm"
  sysvinit: |
    DISTRO_FEATURES:remove = " systemd"
    VIRTUAL-RUNTIME_init_manager = "sysvinit"
